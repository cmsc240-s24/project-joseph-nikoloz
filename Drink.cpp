#include <string>
#include "Consumable.h"
#include "Drink.h"


void sipDrink() 
{
    sipsAmount -= 1;
    if (isEmpty == true)
    {
        throw 
    }
    if (sipsAmount == 0)
    {
        isEmtpy == true;
    }

}